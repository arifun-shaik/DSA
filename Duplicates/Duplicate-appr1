int[] nums = {1,1,2};
        int n = nums.length;
        ArrayList<Integer> list = new ArrayList<>();
        for(int i = 0; i < n ; i++){
            for(int j = i + 1; j < n; j ++){
                if(nums[i] == nums[j])
                list.add(nums[j]);
                
            }

        }
        System.out.println(list);
✅ Time Complexity: O(n²)
✅ Space Complexity: O(n)

